## Goal

Audit and update the Typography reference tab in the Book Editor to ensure all displayed information matches the actual LaTeX PDF output. Also add documentation for new formatting macros.

## Problems to fix

### 1. Font sizes may be out of sync
`web/src/constants/bookTypography.ts` defines H1 as `17.28pt` and H2 as `14.4pt`. These are derived from `\Large` and `\large` at 12pt base. However, a separate task is changing H1 to 16pt and H2 to 13pt via explicit `\fontsize` commands. After that task completes, the constants will be wrong.

**Fix:** Update `bookTypography.ts` to match whatever the LaTeX template actually uses. Check `internal/latex/markdown.go` function `formatHeading` for the actual sizes and update accordingly.

### 2. Verify all typography constants
Cross-check every value in `bookTypography.ts` against the actual LaTeX template (`internal/latex/templates/book.tex`) and markdown converter (`internal/latex/markdown.go`):
- Body text size: currently `12pt` / `14.4pt` line height — verify against `book.tex` line 95
- H1/H2 sizes and weights
- Margins, page dimensions
- List indentation
- Blockquote style
- Paragraph spacing

### 3. Add new formatting macros to Text Styles section
The `STYLE_EXAMPLES` array in `TypographyTab.tsx` (line 194) needs new entries for recently added macros:

- **Text alignment:** `->centered text<-` (center) and `->right-aligned text->` (right)
- **Horizontal rule:** `---` (separator line)
- **Small caps:** `^^SMALL CAPS^^`
- **Forced line break:** `\n` (soft break without new paragraph)
- **Non-breaking space:** `~` (non-breaking space, `\~` for literal tilde)

Add these as new entries in `STYLE_EXAMPLES` with markdown source and rendered preview, similar to existing entries (H1, H2, Body, Lists, Blockquote, Table).

**Note:** Some of these macros may not be implemented yet (they're in pending tasks). Only add entries for macros that are actually implemented at the time this task runs. Check `internal/latex/markdown.go` and `web/src/utils/markdown.ts` to see which macros exist.

## Files to modify

- `web/src/constants/bookTypography.ts` — fix font size values
- `web/src/pages/BookEditor/TypographyTab.tsx` — add new STYLE_EXAMPLES entries
- Possibly `web/src/i18n/locales/{cs,en}/pages.json` — if new translation keys are needed

## Important

- There is a work-in-progress book — no data changes, frontend-only updates
- The typography constants are also used by WYSIWYG preview — make sure they stay consistent

## Verification

1. `./dev.sh` — rebuild
2. Open Book Editor → Typography tab → verify all font sizes match PDF output
3. Verify new macro examples render correctly in preview
4. Export a test PDF → compare heading sizes visually with what Typography tab shows
5. `cd web && npx tsc --noEmit && npm run lint`