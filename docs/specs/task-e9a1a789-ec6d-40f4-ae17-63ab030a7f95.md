# Fix Missing Czech Localization for Photo Sort Options

On the Photos page (`/photos`), the sort dropdown options are displayed in English even when the UI is set to Czech. Options like "Recently Added", "Title", etc. need to be translated.

## Requirements

1. Find the sort options on the Photos page (likely in `web/src/pages/Photos/` or related filter components)
2. Replace hardcoded English strings with i18n translation keys
3. Add Czech translations for all sort options (e.g. "Naposledy přidané", "Název", "Datum", etc.)
4. Add English translations as well for completeness
5. Verify the sort dropdown displays correctly in both Czech and English

## Files to modify

- The Photos page component or filter hook (likely `web/src/pages/Photos/` directory)
- `web/src/i18n/locales/cs/pages.json` — add Czech sort option translations
- `web/src/i18n/locales/en/pages.json` — add English sort option translations