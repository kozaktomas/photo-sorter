\documentclass[a4paper,landscape,twoside]{article}
\usepackage[margin=0mm]{geometry}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{czech}
{{ .BodyFontDeclaration }}
{{ .HeadingFontDeclaration }}
\usepackage[dvipsnames]{xcolor}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{microtype}

% Bleed: extend paper by 3mm on each side with printer crop marks.
\usepackage[cam,noinfo,width=303truemm,height=216truemm,center]{crop}

% Broad printer compatibility
\pdfvariable minorversion 4

% Suppress timestamps for deterministic output
\pdfvariable suppressoptionalinfo 511

\pagestyle{empty}
\pagenumbering{gobble}

% \captionbadge — inline colored badge matching the photo overlay marker style.
% Renders as a TikZ node with explicit minimum_width / minimum_height in mm
% so every badge has identical outer dimensions regardless of which digit it
% contains. The font size scales with the badge size (font_pt = size_mm × 1.5,
% computed in Go), keeping the same text-to-badge ratio at any size.
%
% Vertical alignment: the geometric center of the badge sits 0.5ex above the
% surrounding text baseline (i.e. roughly at the visual center of the x-height).
% Aligning baselines instead would put the badge optically below the text,
% because the badge is symmetric around its center while text sits above
% baseline. The 0.5ex offset uses the SURROUNDING font's ex-height, so it
% auto-scales with caption_font_size.
%   #1 = badge size (mm) — both width and height
%   #2 = font size (pt) — scaled in Go from #1
%   #3 = background color hex (without #)
%   #4 = text color name (white|black) — chosen via WCAG luminance in Go
%   #5 = marker number
\newcommand{\captionbadge}[5]{%
  {\definecolor{cbbg}{HTML}{#3}%
   \tikz[baseline={([yshift=-0.5ex]badge.center)}]{%
     \node[fill=cbbg,text=#4,inner sep=0,
           minimum width=#1mm,minimum height=#1mm,
           font=\fontsize{#2}{#2}\selectfont\bfseries,
           rectangle](badge){#5};%
   }}%
}
% \captionbadgefb — fallback badge when no chapter color is set
% (matches the overlay fallback of black at 55% opacity). Same fixed-dimension
% TikZ implementation and vertical alignment as \captionbadge.
%   #1 = badge size (mm)
%   #2 = font size (pt)
%   #3 = marker number
\newcommand{\captionbadgefb}[3]{%
  \tikz[baseline={([yshift=-0.5ex]badge.center)}]{%
    \node[fill=black!55,text=white,inner sep=0,
          minimum width=#1mm,minimum height=#1mm,
          font=\fontsize{#2}{#2}\selectfont\bfseries,
          rectangle](badge){#3};%
  }%
}

\begin{document}
{{- range $si, $section := .Sections}}
{{- range $pi, $page := $section.Pages}}
% --- Page {{$page.PageNumber}} ({{$page.Style}}) ---
\begin{tikzpicture}[remember picture,overlay,shift={(current page.south west)},x=1mm,y=1mm]
% --- Canvas zone (clipped — expanded by heading bleed for colored boxes) ---
  \begin{scope}
    \clip ({{printf "%.2f" $page.ClipLeftX}},{{printf "%.2f" $page.CanvasBottomY}})
      rectangle ({{printf "%.2f" $page.ClipRightX}},{{printf "%.2f" $page.CanvasTopY}});
{{- range $si, $slot := $page.Slots}}
{{- if $slot.HasPhoto}}
{{- if $slot.IsArchival}}
  % Archival border
  \draw[black!45,line width=0.5pt]
    ({{printf "%.2f" $slot.BorderX}},{{printf "%.2f" $slot.BorderY}})
    rectangle ++({{printf "%.2f" $slot.BorderW}},{{printf "%.2f" $slot.BorderH}});
{{- end}}
  \begin{scope}
    \clip ({{printf "%.2f" $slot.ClipX}},{{printf "%.2f" $slot.ClipY}})
      rectangle ++({{printf "%.2f" $slot.ClipW}},{{printf "%.2f" $slot.ClipH}});
    \node[anchor=south west,inner sep=0]
      at ({{printf "%.2f" $slot.ImgX}},{{printf "%.2f" $slot.ImgY}})
      {\includegraphics[{{$slot.SizeDim}}={{printf "%.2f" $slot.SizeVal}}mm]{{print "{"}}{{$slot.FilePath}}{{print "}"}}};
  \end{scope}
{{- if gt $slot.CaptionMarker 0}}
  % Caption marker
{{- if $slot.ChapterColor}}
  \definecolor{markercolor}{HTML}{ {{- $slot.ChapterColor -}} }
  \fill[markercolor]
{{- else}}
  \fill[black,opacity=0.55]
{{- end}}
    ({{printf "%.2f" $slot.CaptionMarkerX}},{{printf "%.2f" $slot.CaptionMarkerY}})
    rectangle ++({{printf "%.2f" $slot.CaptionMarkerSize}},{{printf "%.2f" $slot.CaptionMarkerSize}});
  \node[anchor=center,inner sep=0,font=\fontsize{ {{- printf "%.2f" $slot.CaptionMarkerFontSize -}} }{ {{- printf "%.2f" $slot.CaptionMarkerFontSize -}} }\selectfont\bfseries,text={{contrastTextColor $slot.ChapterColor}}]
    at ({{printf "%.2f" $slot.CaptionMarkerCenterX}},{{printf "%.2f" $slot.CaptionMarkerCenterY}})
    {{print "{"}}{{$slot.CaptionMarker}}{{print "}"}};
{{- end}}
{{- else if $slot.HasText}}
  % Text slot ({{$slot.TextType}})
  \node[anchor=north west,inner sep=0]
    at ({{printf "%.2f" (addFloat $slot.ClipX $slot.TextPadLeft)}},{{printf "%.2f" (addFloat $slot.ClipY $slot.ClipH)}})
    {\parbox{{print "{"}}{{printf "%.2f" (subtractFloat (subtractFloat $slot.ClipW $slot.TextPadLeft) $slot.TextPadRight)}}mm}{%
      \parskip=0pt\parindent=0pt\fontsize{ {{- printf "%.0f" $.BodyFontSize -}} }{ {{- printf "%.0f" $.BodyLineHeight -}} }\selectfont{{if $slot.RaggedRight}}\raggedright{{end}}%
      {{markdownToLatexColor $slot.TextContent $slot.ChapterColor $slot.BleedLeftMM $slot.BleedRightMM}}%
    }};
{{- else if $slot.HasCaptionsList}}
  % Captions slot — page's photo captions stacked vertically with hanging
  % indent so wrapped lines align under the caption text (not under the
  % badge). Each caption is its own paragraph; the badge sits flush left and
  % wrapped lines are indented by ~badge width + thin gap. The bottom
  % captions strip is suppressed for this page in Go.
  {\microtypesetup{expansion=false}%
  \node[anchor=north west,inner sep=0]
    at ({{printf "%.2f" $slot.ClipX}},{{printf "%.2f" (addFloat $slot.ClipY $slot.ClipH)}})
    {\parbox{{print "{"}}{{printf "%.2f" $slot.ClipW}}mm}{%
      \parskip=0.5\baselineskip\parindent=0pt%
      \fontsize{ {{- printf "%.0f" $.CaptionFontSize -}} }{ {{- printf "%.0f" $.CaptionLeading -}} }\selectfont
      \color{black!{{$.CaptionOpacity}}}
      {{- range $ci, $cap := $slot.CaptionsList}}
      {{if $ci}}\par {{end}}\noindent\hangindent={{printf "%.2f" (slotCaptionIndentMM $.CaptionBadgeSize)}}mm\relax {{renderSlotCaption $cap $.CaptionBadgeSize}}
      {{- end}}
    }};
  }%
{{- end}}
{{- end}}
  \end{scope}
% --- Footer zone ---
{{- if $page.HasCaptions}}
  % Captions footer.
  %
  % Layout strategy:
  %   - Each caption (badges + text) is wrapped in \mbox{} so it never breaks
  %     mid-sentence. The only break point between captions is the \quad glue.
  %   - align=flush left disables justification. Without it, TeX would try to
  %     justify, fail to find stretchable glue (mbox + \quad are both rigid),
  %     and produce an overfull hbox instead of breaking between captions.
  %   - \microtypesetup{expansion=false} disables microtype's font expansion
  %     for this block. Otherwise microtype silently shrinks glyphs by ~2% to
  %     squeeze a tight caption line within the target width, leaving the
  %     line crammed against the right edge with no breathing room.
  %   - Caption text may contain newlines (\n in the source string). Each
  %     newline becomes a hard \\ break inside the caption block: inline
  %     newlines split a caption into stacked mbox segments, a trailing
  %     newline forces the next caption to start on a new line. The full
  %     LaTeX is built in renderFooterCaptionsLatex (latex.go) so the
  %     template stays readable.
  {\microtypesetup{expansion=false}%
  \node[anchor=north west,inner sep=0,text width={{printf "%.2f" $page.CaptionBlockW}}mm,
    align=flush left,
    font=\fontsize{ {{- printf "%.0f" $.CaptionFontSize -}} }{ {{- printf "%.0f" $.CaptionLeading -}} }\selectfont,text=black!{{$.CaptionOpacity}}]
    at ({{printf "%.2f" $page.CaptionBlockX}},{{printf "%.2f" $page.CaptionBlockY}})
    { {{- renderFooterCaption $page.Captions $.CaptionBadgeSize -}} };
  }%
{{- end}}
{{- if not $page.HidePageNumber}}
  \node[anchor={{$page.FolioAnchor}},inner sep=0,font=\fontsize{ {{- printf "%.0f" $.CaptionFontSize -}} }{ {{- printf "%.0f" $.CaptionLeading -}} }\selectfont,text=black!85]
    at ({{printf "%.2f" $page.FolioX}},{{printf "%.2f" $page.FolioY}})
    {{print "{"}}{{$page.PageNumber}}{{print "}"}};
{{- end}}
{{- if $.DebugOverlay}}
% --- Debug overlay ---
  \draw[blue!40,line width=0.3pt]
    ({{printf "%.2f" $page.ContentLeftX}},{{printf "%.2f" $page.CanvasBottomY}})
    rectangle ({{printf "%.2f" $page.ContentRightX}},{{printf "%.2f" $page.CanvasTopY}});
  \draw[green!40,line width=0.2pt,dashed]
    ({{printf "%.2f" $page.ContentLeftX}},{{printf "%.2f" $page.HeaderY}})
    -- ({{printf "%.2f" $page.ContentRightX}},{{printf "%.2f" $page.HeaderY}});
  \draw[green!40,line width=0.2pt,dashed]
    ({{printf "%.2f" $page.ContentLeftX}},{{printf "%.2f" $page.FooterRuleY}})
    -- ({{printf "%.2f" $page.ContentRightX}},{{printf "%.2f" $page.FooterRuleY}});
{{- range $off := $.DebugColOffsets}}
  \draw[red!25,line width=0.2pt]
    ({{printf "%.2f" (addFloat $page.ContentLeftX $off)}},{{printf "%.2f" $page.CanvasBottomY}})
    -- ({{printf "%.2f" (addFloat $page.ContentLeftX $off)}},{{printf "%.2f" $page.CanvasTopY}});
{{- end}}
{{- end}}
\end{tikzpicture}
{{- if not $page.IsLast}}
\newpage
{{- end}}
{{- end}}
{{- end}}
\end{document}
